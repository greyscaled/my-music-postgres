version: 1.0.{build}
branches:
  except:
    - config
image: ubuntu
services:
  - postgresql
install:
  - chmod u+x my-music
  - set PGUSER=postgres
  - set PGPASSWORD=Password12!
  - createdb MyMusic
build: off
test_script:
  - ./my-music --help
